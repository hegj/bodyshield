<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                xmlns:app="http://schemas.android.com/apk/res-auto"
                android:layout_width="match_parent"
                android:layout_height="50dp"
                android:minHeight="50dp"
                android:background="@drawable/device_item_pressed"
    >
    <!--
    <ImageView
        android:layout_width="@dimen/list_item_width"
        android:layout_height="@dimen/list_item_height"
        android:layout_marginStart="@dimen/device_item_margin"
        android:layout_centerVertical="true"
        android:id="@+id/userImageBtn"
        android:scaleType="fitXY"
        android:src="@mipmap/people"/>
    -->
    <com.cmax.bodysheild.widget.CircleImageView
        android:layout_width="@dimen/list_item_width"
        android:layout_height="@dimen/list_item_height"
        android:layout_marginStart="@dimen/device_item_margin"
        android:layout_centerVertical="true"
        android:id="@+id/userImageBtn"
        android:src="@mipmap/people"
        app:border_width="1dp"
        app:border_color="#70C8EF" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/deviceUser"
        android:layout_toRightOf="@id/userImageBtn"
        android:layout_marginStart="15dp"
        android:textColor="@color/navigation_bar_text"
        android:background="@android:color/transparent"
        android:layout_centerVertical="true"
        />

    <ImageView
        android:layout_width="@dimen/list_item_width"
        android:layout_height="@dimen/list_item_height"
        android:id="@+id/checkedView"
        android:layout_alignParentEnd="true"
        android:src="@mipmap/check01"
        android:layout_marginRight="8dp"
        android:layout_centerVertical="true"
        android:visibility="gone"/>
</RelativeLayout>